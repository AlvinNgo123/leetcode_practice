Write a function that given a string, determines if it is a palindrome. 
A palindrome is defined as a string that's written the same forward and backward.
For this problem, we can assume that empty and single-character strings are palindromes.
