Write a function that, given a string "s" that contains just '(', ')', '{', '}', '[' and ']', returns if input string is valid.
A valid input string must have the 2 following characteristics.
1) Open brackets must be closed by the same type of brackets.
2) Open brackets must be closed in the correct order (those open latest must be closed before prior opened ones are closed).